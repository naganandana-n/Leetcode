'''
2683. Neighboring Bitwise XOR
'''

class Solution:
    def doesValidArrayExist(self, derived: List[int]) -> bool:
        ans = [0] * len(derived)
        for i, j in enumerate(ans):
            if i == len(dervied) - 1:
                if j 
            if j ^ ans[i+1] == derived[i]:
                continue
            elif 